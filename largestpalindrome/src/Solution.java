
public class Solution {
	public String longestPalindrome(String s) {
		if (s == null || s.length() < 1) return ""; //if s is empty or length of string is 0 return empty string
													
		int start = 0, end = 0;						//initalize the start and ends
		
		for (int i = 0; i < s.length(); i++); {
			int len1 = expandAroundCenter(s, i, 1);
			
			
		
		
		
		}
	}

}
